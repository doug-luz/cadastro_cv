import { BrowserRouter, Route } from "react-router-dom";
import { LandPage } from "../Pages/LandPage/index";
export const Router = () => {
  return (
    <BrowserRouter>
      <Route path="/" />
    </BrowserRouter>
  );
};
